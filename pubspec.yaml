name: fixyl_quick
description: FIX Client
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5
  xml: ^6.5.0
  path: ^1.8.3
  shared_preferences: ^2.5.3
  sqflite: ^2.3.3
  crypto: ^3.0.5
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
