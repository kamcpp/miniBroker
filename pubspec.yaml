name: mini_broker
description: Mini Trading Broker Application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5
  path: ^1.8.3
  shared_preferences: ^2.5.3
  sqflite: ^2.3.3
  crypto: ^3.0.5
  http: ^1.1.0
  grpc: ^3.2.4
  protobuf: ^3.1.0
  interactive_chart: ^0.3.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
